
import * as types from '../mutation-types'
import { api } from '../../api'

const state = {
  groupList: []
}

const getters = {}

const actions = {}

const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
